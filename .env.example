# MongoDB connection string
# For local Docker Compose, use the default below (mongo service is included)
MONGODB_URI=mongodb://mongo:27017/apk-scanner

# Dynamic analysis timeout in seconds (default: 120)
DYNAMIC_TIMEOUT=120
